# Tesseract models

`eng.traineddata` (tessdata_fast) and `rus.traineddata` (tessdata_best) go
here. They are not in git; download them from the repository root with:

```bash
./scripts/download_tessdata.sh
```

Every file in this directory is packaged into the APK.
